{
  "name": "editpro",
  "version": "1.0.0",
  "description": "EditPro — rename files in a folder to a numeric sequence",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}